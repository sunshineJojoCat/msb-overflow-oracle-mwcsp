# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/

# macOS
.DS_Store

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.bbl
paper/*.blg
paper/*.fdb_latexmk
paper/*.fls
paper/*.synctex.gz
paper/*.out
paper/*.toc

# Local manuscript working copy (kept here so the paper can be compiled inside
# this repo for convenience; the manuscript itself is distributed through the
# journal and must NOT be committed to this public reproducibility repo)
paper/

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Misc
.pytest_cache/
